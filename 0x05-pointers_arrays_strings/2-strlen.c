#include "main.h"

/**
 * _strlen - returns the lenght of string
 *
 * @s: character to getstring
 * Return: returns lenght
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{}
	return (i);
}
